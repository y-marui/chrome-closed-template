> This is the reference (English) version.

- Permission minimization: request only what is needed
- host_permissions minimization: avoid broad patterns like `<all_urls>`
- Remote code forbidden: do not fetch and execute external scripts
- Inline scripts forbidden: no `<script>` blocks or `onclick` attributes in HTML
